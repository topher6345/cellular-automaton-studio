import { WorkerFunctionProps, ITypedWorker } from './typed-worker';
export interface ICreateWorkerProps<Input, Output, State = any> {
    workerFunction: (props: WorkerFunctionProps<Input, Output, State>) => void;
    onMessage?: (output: Output) => void;
    onError?: (error: ErrorEvent) => void;
    importScripts?: string[];
}
export declare function createWorker<Input, Output, State = any>(props: ICreateWorkerProps<Input, Output, State>): ITypedWorker<Input, Output>;
